import React from "react";
import { Container, Typography } from '@material-ui/core';

const Home: React.FC = () => (
    <Container>
      <Typography>Home</Typography>
    </Container>
  )

export default Home;
